# @title_es Establecer UTC como zona horaria del servidor
# @title_en Set UTC as the server timezone
# @input ln -s /usr/share/zoneinfo/UTC /etc/localtime
- name: set UTC timezone
  file:
    src: "/usr/share/zoneinfo/UTC"
    dest: "/etc/localtime"
    state: link
  notify:
  - Restart Cron
