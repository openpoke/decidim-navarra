---
## Various
rails_env: "{{env}}"
app_base_path: /var/www/{{app_id}}
app_group: www-data
background_jobs_adapter: sidekiq

## Database
site_db_name: webparticipacionciudadana
site_db_user: usr_webparticipacionciudadana
site_db_host: dc1gvalpsg004
site_db_port: 5432

## Web server
http_auth_enabled: False #TODO True
http_auth_file: "{{app_id}}_auth"
http_auth_user: decidim
http_auth_password: staging123
https_enabled: False #TODO True
ssl_certificate_name: "{{app_id}}"
ssl_certificate: /etc/letsencrypt/live/{{ssl_certificate_name}}/fullchain.pem
ssl_certificate_key: /etc/letsencrypt/live/{{ssl_certificate_name}}/privkey.pem
nginx_access_log: /var/log/nginx/{{app_id}}_access.log
nginx_error_log: /var/log/nginx/{{app_id}}_error.log

## Application
rollbar_access_token:
redis_url:
logrotate_frequency: 30

## Services
geocoder_lookup_app_id:
geocoder_lookup_app_code:
